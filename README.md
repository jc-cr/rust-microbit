# rust-microbit

`cargo new' (your package name)
`rustup target add thumbv7em-none-eabihf`

confirm with `rustup show`

make stuff in directory

cargo embed --chip nRF51822_xxAA

it will build and flash the microbit
